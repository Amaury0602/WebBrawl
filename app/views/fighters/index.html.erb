<div class="container">

  <div>
    <h1>Click on the hero you want to fight with</h1>
  </div>

  <div class="index-content">
    <div class="fighter-cards">
      <% @fighters.each do |fighter| %>
        <%= render "show", fighter: fighter %>
      <% end %>
    </div>

    <div class="index-links">
      <%= link_to "Create your own fighter", new_fighter_path %>
      <%= link_to "See battle history", battles_path  %>
      <div class="create-battle">
        <%= form_for @battle do |f| %>
          <%= f.hidden_field :your_fighter_id, input_html: {
            value: '',
            id: "your-fighter"
          } %>
          <%= f.hidden_field :enemy_fighter_id, input_html: {
            value: 'qefq',
            id: "enemy-fighter"
          } %>
          <%= f.submit %>
        <% end %>
      </div>
    </div>
  </div>
</div>





